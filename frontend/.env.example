# 数据模式：mock（内置演示数据）/ http（调用 ColdPilot 后端）。
VITE_DATA_MODE=mock

# 仅 http 模式生效：后端 API 的 base URL。
# - 同源部署留空（默认）：前端请求 /api/v1/...
# - 跨源/本地联调示例：VITE_COLDPILOT_API_BASE_URL=http://localhost:8000
VITE_COLDPILOT_API_BASE_URL=
